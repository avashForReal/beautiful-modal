import { 
  Music, 
  Guitar, 
  AudioWaveform, 
  Zap, 
  LockOpen,
  Type,
  Blend,
  Replace,
  LogOut,
  Rows2
} from "lucide-react";

export const iconMap: Record<string, React.ElementType> = {
  music: Music,
  guitar: Guitar,
  "audio-waveform": AudioWaveform,
  zap: Zap,
  "lock-open": LockOpen,
  type: Type,
  blend: Blend,
  replace: Replace,
  "log-out": LogOut,
  "rows-2": Rows2,
};